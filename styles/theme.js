// Valores de layout que NO dependen del tema (no cambian con dark/light)
// Para colores usa: import { useTheme } from '../context/ThemeContext'

export const spacing = {
  xs:  4,
  sm:  8,
  md:  14,
  lg:  20,
  xl:  24,
  xxl: 32,
};

// Padding horizontal estándar de pantalla
export const HP = 20;

export const radii = {
  sm:  8,
  md:  12,
  lg:  16,
  xl:  20,
  xxl: 24,
};

export const fontSizes = {
  xs:  12,
  sm:  14,
  md:  16,
  lg:  18,
  xl:  22,
  xxl: 28,
};

// Altura fija del header en pantallas (status bar + contenido)
export const HEADER_TOP = 56;
